image: alpine:latest

pages:
  stage: deploy
  tags:
    - static-site
  script:
    - mkdir -p public
    - cp index.html public/
    - cp -r assets public/ 2>/dev/null || true
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
