image: alpine:latest

pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp *.html *.svg public/
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
